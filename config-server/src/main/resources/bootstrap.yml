spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:/D:/spring/Projects/Event-Driven-Microservices/event-driven-microservices/config-server-respository
          clone-on-start: true
      fail-fast: true